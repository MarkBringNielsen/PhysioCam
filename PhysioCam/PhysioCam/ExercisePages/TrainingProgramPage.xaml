<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:PhysioCam.Views;assembly=PhysioCam"
             x:Class="PhysioCam.ExercisePages.TrainingProgramPage"
             
   >
    <ContentPage.Content>
        <StackLayout Padding="10">
            <StackLayout HorizontalOptions="CenterAndExpand" Orientation="Horizontal" Padding="0,10,0,50">


                <Button  Text="First Exercise"                     
                         
                         Clicked="OnButtonClicked"/>
                <Button  Text="Stretches"                     
                         
                         Clicked="OnButtonClicked"/>
                    <!--<Button  Text="Last Exercise" 
                             Style="{StaticResource MainStyleButton}"
                             Clicked="OnButtonClicked"/>-->
            </StackLayout>
            
            <views:DescriptionView/>
            
            <StackLayout HorizontalOptions="CenterAndExpand" Orientation="Horizontal">
                <Button Text="Cancel" Clicked="CancelButtonOnClicked"/>
                <Button Text="Save and quit" Clicked="SaveAndQUitButtonOnClicked"/>
                <Button Text="Send" Clicked="DoneButtonOnClicked"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>